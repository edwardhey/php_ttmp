// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("ttmp", "for ttmp support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("ttmp", "enable ttmp support", "no");

if (PHP_TTMP != "no") {
	EXTENSION("ttmp", "ttmp.c");
}

